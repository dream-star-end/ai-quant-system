# AI Quant System 🤖📈

**AI 驱动的智能量化交易平台** — 支持 A 股和加密货币市场的数据分析、策略回测、模拟交易、风险管理和 AI 智能推荐。

## ✨ 核心功能

### 📊 市场数据
- **A 股行情**: 沪深300、茅台、宁德时代等热门标的实时报价和历史 K 线
- **加密货币**: BTC、ETH、SOL 等主流币种实时行情 (Binance/OKX/Huobi)
- **全量技术指标**: MA/RSI/MACD/布林带/KDJ/成交量均线

### 🧪 策略回测引擎
6 种内置量化策略，支持自定义参数回测:
| 策略 | 描述 |
|------|------|
| 均线交叉 (MA Cross) | 快慢均线金叉/死叉 |
| RSI 超买超卖 | RSI 在超买超卖区域交易 |
| MACD 信号 | MACD 与信号线交叉 |
| 布林带 (Bollinger) | 价格触及布林带上下轨 |
| Dual Thrust | 经典日内突破策略 |
| 海龟交易 (Turtle) | 经典趋势跟踪 |

回测输出: 收益率、夏普比率、最大回撤、胜率、盈亏比、权益曲线、回撤曲线、月度收益、交易明细

### 🤖 AI 智能分析
- **多信号融合预测**: 均线趋势 + RSI + MACD + 成交量 + 动量综合分析
- **智能投资推荐**: 操作建议、仓位建议、止损/止盈价
- **风险评估**: 夏普比率、Sortino、VaR、CVaR、最大回撤、风险评分

### 💼 投资组合管理
- 模拟交易 (Paper Trading)
- 买入/卖出执行
- 持仓管理 + 浮动盈亏
- 自动止损/止盈
- 风控检查 (仓位限制)

### 🔔 智能告警
- 价格告警 (上穿/下穿)
- 涨跌幅告警
- 量能异动
- 策略信号提醒
- 风控预警
- 回撤预警

### 👤 用户系统
- JWT 认证注册/登录
- 自选列表管理
- 个人策略库
- 操作日志

## 🏗 技术架构

```
┌────────────────────────────────────────────────┐
│                  Frontend                       │
│           Vue 3 + Vite + Element Plus           │
│         ECharts K线图 + 响应式暗色主题          │
├────────────────────────────────────────────────┤
│                  Backend                        │
│              FastAPI + Uvicorn                   │
│  ┌──────────┬───────────┬──────────┬─────────┐ │
│  │ 市场数据  │ 回测引擎  │ AI分析   │ 风控    │ │
│  │ yfinance │ 6种策略   │ 多信号   │ 仓位    │ │
│  │ ccxt     │ 权益曲线  │ 融合预测 │ 止损    │ │
│  └──────────┴───────────┴──────────┴─────────┘ │
├────────────────────────────────────────────────┤
│                 Database                        │
│          Supabase (PostgreSQL + RLS)            │
│  11张表: 用户/组合/持仓/策略/交易/告警/...      │
└────────────────────────────────────────────────┘
```

## 🚀 快速开始

### 1. 后端

```bash
# 安装依赖
pip install -r requirements.txt

# 配置环境变量
cp .env.example .env
# 编辑 .env 填入你的 Supabase URL 和 Key

# 启动服务
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

API 文档: http://localhost:8000/docs

### 2. 前端

```bash
cd frontend
npm install
npm run dev
```

访问: http://localhost:3000

### 3. Docker 部署

```bash
# 设置环境变量
export SUPABASE_URL=your-url
export SUPABASE_KEY=your-key
export SECRET_KEY=your-secret

# 启动
docker-compose up -d
```

## 📁 项目结构

```
ai-quant-system/
├── backend/
│   ├── main.py              # FastAPI 入口
│   ├── config.py            # 配置管理
│   ├── database.py          # Supabase 客户端
│   ├── core/                # 核心模块
│   │   ├── logger.py        # 日志系统
│   │   ├── exceptions.py    # 异常处理
│   │   └── security.py      # JWT 认证
│   ├── routers/             # API 路由 (9个模块)
│   │   ├── auth.py          # 认证
│   │   ├── stocks.py        # A股数据
│   │   ├── crypto.py        # 加密货币
│   │   ├── analysis.py      # AI分析
│   │   ├── backtest.py      # 策略回测
│   │   ├── strategies.py    # 策略管理
│   │   ├── portfolio.py     # 投资组合
│   │   ├── alerts.py        # 智能告警
│   │   └── watchlist.py     # 自选管理
│   ├── services/            # 业务服务
│   │   ├── market_data.py   # 市场数据服务
│   │   ├── backtest_engine.py  # 回测引擎
│   │   ├── risk_manager.py  # 风险管理
│   │   └── ai_service.py    # AI 分析服务
│   ├── schemas/             # Pydantic 数据模型
│   └── models/              # 数据库模型
├── frontend/
│   ├── src/
│   │   ├── views/           # 7个页面
│   │   ├── services/api.js  # API 客户端
│   │   ├── stores/          # Pinia 状态管理
│   │   └── router/          # Vue Router
│   └── package.json
├── models/                  # AI 模型
├── data/                    # 数据采集
├── tests/                   # 测试
├── docker-compose.yml       # Docker 编排
├── Dockerfile               # 后端镜像
└── requirements.txt         # Python 依赖
```

## 📊 API 接口概览

| 模块 | 端点 | 说明 |
|------|------|------|
| 认证 | `POST /api/v1/auth/register` | 注册 |
| 认证 | `POST /api/v1/auth/login` | 登录 |
| A股 | `GET /api/v1/stocks/quote/{symbol}` | 实时报价 |
| A股 | `GET /api/v1/stocks/history/{symbol}` | K线+指标 |
| A股 | `GET /api/v1/stocks/batch` | 批量报价 |
| 加密货币 | `GET /api/v1/crypto/price/{symbol}` | 实时价格 |
| 加密货币 | `GET /api/v1/crypto/history/{symbol}` | K线+指标 |
| AI分析 | `GET /api/v1/analysis/predict/{symbol}` | 趋势预测 |
| AI分析 | `GET /api/v1/analysis/recommend/{symbol}` | 智能推荐 |
| AI分析 | `GET /api/v1/analysis/risk/{symbol}` | 风险评估 |
| 回测 | `POST /api/v1/backtest/run/guest` | 运行回测 |
| 策略 | `GET/POST /api/v1/strategies/` | 策略CRUD |
| 组合 | `POST /api/v1/portfolio/trade` | 执行交易 |
| 告警 | `GET/POST /api/v1/alerts/` | 告警管理 |

完整 API 文档请访问 `/docs` (Swagger UI)

## 🔧 数据库 (Supabase)

使用 Supabase 托管 PostgreSQL，包含以下表:

- `users` - 用户
- `portfolios` - 投资组合
- `positions` - 持仓
- `strategies` - 策略
- `strategy_signals` - 策略信号
- `trades` - 交易记录
- `alerts` - 告警
- `backtest_results` - 回测结果
- `market_data_cache` - 行情缓存
- `watchlists` - 自选列表
- `activity_logs` - 操作日志

全部表启用了 RLS (Row Level Security) 和自动 `updated_at` 触发器。

## 📄 许可证

MIT
